name = "example/stream_guard"

version = "1.4.0"

readme = "README.md"

repository = "https://github.com/example/stream_guard"

license = "Apache-2.0"

preferred_target = "js"

supported_targets = "js"
